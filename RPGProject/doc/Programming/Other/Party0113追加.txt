フィールドに定数として以下を追加。
public const int ITEM_MAXNUM = 50; //50は暫定。パーティが持てるアイテムの最大数。
public const int MEMBER_MAXNUM = 4; //パーティの最大メンバー数
private string[] item; //パーティ全体が管理するアイテムを表します。
private int memNum; //現在のメンバー数を表します。

コンストラクタで、item = new string[MEMBER_MAXNUM]; で配列を生成

AddMember変更 : 仕様書の通りに書いてください

RemoveMember追加 : 仕様書参照

SearchPlayer → SearchPlayerUnit に名称変更
戻り値もPlayer → PlayerUnit に変更